pipeline {
agent {
  label 'jenkins-1'
}
    stages {
        stage('Hello') { // Defines a stage named 'Hello'
            steps {
                echo 'Hello World' // Prints "Hello World" to the console output
            }
        }
    }
}
